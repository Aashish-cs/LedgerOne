services:
  - type: web
    name: ledgerone-api
    runtime: docker
    plan: free
    branch: main
    autoDeployTrigger: commit
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /actuator/health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ledgerone-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: "https://ledger-one-mocha.vercel.app,http://localhost:5173,http://127.0.0.1:5173"
      - key: MARKET_SIMULATOR_ENABLED
        value: "true"
      - key: MARKET_LIVE_PRICES_ENABLED
        value: "true"
      - key: MARKET_QUOTE_CACHE_SECONDS
        value: "60"
      - key: FINNHUB_API_KEY
        sync: false
      - key: MARKET_QUOTE_URL_TEMPLATE
        value: "https://api.nasdaq.com/api/quote/{symbol}/info?assetclass=stocks"
      - key: ACCESS_TOKEN_MINUTES
        value: "20"
      - key: REFRESH_TOKEN_DAYS
        value: "7"

databases:
  - name: ledgerone-db
    databaseName: ledgerone
    user: ledgerone
    plan: free
